# 🐾 [과제 완벽 대비] 내가 알고 있어야 하는 핵심 답변 및 구현 보고서

> **프로젝트명**: Pet Research Navigator (반려동물 연구논문 및 시계열 연구동향 AI 비서)  
> **저장소**: [https://github.com/haru2014/hj09m02](https://github.com/haru2014/hj09m02)  
> **기술 스택**: FastAPI, Pydantic v2, Google Gemini API, Firebase Firestore, Chart.js, Vanilla HTML5/CSS3/ES6+  
> **배포 환경**: Backend (Render), Frontend (Vercel)

---

## 📌 목차
1. [1분 프로젝트 핵심 요약 (엘리베이터 피치)](#1-1분-프로젝트-핵심-요약-엘리베이터-피치)
2. [과제 목표 6대 핵심 질문 및 모범 답변](#2-과제-목표-6대-핵심-질문-및-모범-답변)
   - [Q1. 시계열 데이터 분석 및 요약 활용 흐름](#q1-시계열-데이터-분석-및-요약-활용-흐름)
   - [Q2. FastAPI 계층형 아키텍처 분리 기준](#q2-fastapi-계층형-아키텍처-분리-기준)
   - [Q3. Pydantic을 활용한 데이터 검증 이유와 방식](#q3-pydantic을-활용한-데이터-검증-이유와-방식)
   - [Q4. Firestore NoSQL 데이터베이스 CRUD 연동 원리](#q4-firestore-nosql-데이터베이스-crud-연동-원리)
   - [Q5. 데이터 요약 컨텍스트 주입(Context Injection) 원리](#q5-데이터-요약-컨텍스트-주입context-injection-원리)
   - [Q6. 배포 환경에서의 CORS, 환경 변수, 보안 관리](#q6-배포-환경에서의-cors-환경-변수-보안-관리)
3. [구현된 보너스 과제(가산점 항목) 및 기술적 차별점](#3-구현된-보너스-과제가산점-항목-및-기술적-차별점)
4. [주요 엔드포인트 및 데이터 구조 요약](#4-주요-엔드포인트-및-데이터-구조-요약)
5. [면접/평가 예상 꼬리질문 대비 FAQ](#5-면접평가-예상-꼬리질문-대비-faq)

---

## 1. 1분 프로젝트 핵심 요약 (엘리베이터 피치)

> **"어떤 프로젝트를 개발하셨나요?" 질문에 대한 답변:**
>
> "일반적인 ChatGPT 같은 범용 LLM은 개별 연구자의 특화 데이터나 시계열 연구 데이터를 알지 못해 상투적인 답변만 제공합니다.  
> 저는 이 문제를 해결하기 위해 **16개년(2010~2025)에 걸친 112건의 시계열 연구 데이터와 154편의 수의학 구조화 논문을 분석하여, 질문 발생 시 통계 요약과 대표 논문을 LLM 시스템 프롬프트에 실시간 주입하는 'Pet Research Navigator'를 구축**했습니다.  
> 
> 백엔드는 **FastAPI**와 **Pydantic v2**, **Google Gemini 3.5 Flash**를 연동하고, 클라우드 **Firestore**와 로컬 인메모리 폴백의 듀얼 레이어 저장소를 설계했습니다. 프론트엔드는 프레임워크 없이 **순수 바닐라 JS와 Chart.js**로 구현하여 시계열 추세 시각화, CRUD 데이터 관리, 이전 대화 불러오기 복원, CSV/JSON 내보내기, 다크 모드까지 완성하여 Render와 Vercel에 분리 배포했습니다."

---

## 2. 과제 목표 6대 핵심 질문 및 모범 답변

### Q1. 시계열 데이터 분석 및 요약 활용 흐름
**Q: 시계열 데이터를 어떻게 분석하고, 요약 정보를 만들어 AI 서비스에서 활용했나요?**
- **답변 핵심**:
  1. **데이터 수집 및 정렬**: 7대 연구 분야(관절, 행동, 피부, 질병, 심장, 영양, 노령)의 2010~2025년 16개년 시계열 데이터를 날짜순(`YYYY-MM`)으로 정렬합니다.
  2. **통계 및 지표 산출 (`compute_data_summary`)**:
     - **기본 통계**: 총합(Total), 연평균(Average), 최고치(Max), 최저치(Min).
     - **트렌드 증감률 계산**: 초기 30% 구간의 평균과 최근 30% 구간의 평균을 비교하여 성장률을 계산합니다 (예: 관절 분야의 경우 `+284.8% 지속 상승`).
     - **방법론 메타데이터 매핑**: 세대별(2010~2015 전통 진단 ➔ 2016~2020 압력판 ➔ 2021~2025 AI/웨어러블) 핵심 연구 기법을 도출합니다.
  3. **서비스 활용**: 프론트엔드의 4대 요약 카드와 Chart.js 차트에 즉시 시각화되며, 사용자가 질문할 때 백엔드 `/api/data/summary`를 통해 LLM 프롬프트에 자동 전달됩니다.

---

### Q2. FastAPI 계층형 아키텍처 분리 기준
**Q: FastAPI 프로젝트를 라우터와 서비스 등으로 분리해 구성한 기준과 장점은 무엇인가요?**
- **답변 핵심**:
  - **관심사의 분리(Separation of Concerns, SoC)** 원칙에 따라 단일 책임 원칙(SRP)을 준수하도록 모듈화했습니다.
  - **디렉토리별 역할**:
    - `backend/main.py`: FastAPI 앱 인스턴스 초기화, CORS 미들웨어 등록, 라우터 취합 및 프론트엔드 정적 서빙.
    - `backend/api/`: HTTP 요청 및 응답 라우팅 담당 (`data.py`, `chat.py`, `conversations.py`, `papers.py`).
    - `backend/services/`: 핵심 비즈니스 로직 담당 (`ai_service.py` - Gemini 호출 및 폴백, `analytics_service.py` - 시계열 통계 계산, `firestore_service.py` - DB 접근).
    - `backend/models/schemas.py`: Pydantic 스키마 정의 (요청/응답 DTO).
    - `backend/data/`: 112개 시계열 데이터와 154편 구조화 논문 시드 데이터.
  - **장점**: 라우터 코드가 비대해지는 것을 방지하고, AI 엔진 변경이나 DB 마이그레이션 시 비즈니스 로직만 수정하면 되므로 유지보수성이 극대화됩니다.

---

### Q3. Pydantic을 활용한 데이터 검증 이유와 방식
**Q: Pydantic을 활용해 요청 데이터 검증을 적용한 이유와 실제 동작 방식은?**
- **답변 핵심**:
  - **적용 이유**: 런타임 에러 사전 방지, 잘못된 타입의 입력 거부, 데이터 무결성 보장, 그리고 Swagger UI(/docs)에 자동 문서화 제공.
  - **실제 적용 방식**:
    - `DataCreate`: `date` 필드가 `YYYY-MM` 또는 `YYYY-MM-DD` 정규식 패턴과 일치하는지 검증하고, `value`가 음수가 되지 않도록 `ge=0` 제약 조건을 설정했습니다.
    - `ChatRequest`: 빈 문자열 질문을 차단하고 옵셔널 `topic`, `conversation_id` 타입을 명시했습니다.
    - `ChatResponse`: 생성된 답변뿐만 아니라 사용된 데이터 요약(`summary_used`), 추천 후속 질문, 실제 사용된 AI 엔진 명칭(`engine_used`)을 타입 안전하게 반환합니다.
  - **검증 실패 시**: FastAPI가 비정상 입력을 감지하여 컨트롤러 로직 진입 전에 표준 `422 Unprocessable Entity` JSON 에러를 자동 반환합니다.

---

### Q4. Firestore NoSQL 데이터베이스 CRUD 연동 원리
**Q: Firestore에 데이터를 어떻게 저장하고 CRUD로 다루었으며, 인증 키가 없을 때는 어떻게 대응했나요?**
- **답변 핵심**:
  - **컬렉션 구조**:
    - `data`: 112건의 시계열 연구 데이터 `(id, date, value, memo, topic)`
    - `conversations`: 세션별 Q&A 대화 내역 `(id, title, topic, messages, created_at, updated_at)`
    - `papers`: 154편의 구조화된 논문 메타데이터 `(id, title, year, species, topic, method, summary)`
  - **CRUD 조작**: `firebase-admin` 공식 SDK의 `collection().document().set()`, `.get()`, `.update()`, `.delete()`, `.stream()`을 활용했습니다.
  - **듀얼 레이어 폴백 (Dual-Layer Architecture)**:
    - 클라우드 배포 시에는 서비스 계정 키(`FIREBASE_SERVICE_ACCOUNT_JSON` / `FIREBASE_SERVICE_ACCOUNT_PATH`)로 실제 Firestore에 접속합니다.
    - 로컬 환경이나 키 미제공 시에는 `LocalStore` 인메모리/스마트 저장소로 자동 전환되어 서비스가 중단 없이 완벽하게 구동됩니다.

---

### Q5. 데이터 요약 컨텍스트 주입(Context Injection) 원리
**Q: “데이터 요약을 시스템 프롬프트에 주입하는 방식(컨텍스트 주입)”의 원리와 장점은?**
- **답변 핵심**:
  - **원리**: 사용자가 "관절 연구동향 요약해줘"라고 질문하면,
    1. 질문 키워드로부터 연구 주제(`관절`)를 자동 판별합니다.
    2. DB에서 해당 주제의 16개년 데이터와 논문을 조회하고, `compute_data_summary()`를 통해 통계 요약본을 생성합니다.
    3. `SYSTEM_PROMPT_TEMPLATE`의 `{topic}`, `{period}`, `{count}`, `{metrics_total}`, `{trend}`, `{key_methods}`, `{papers_context}` 위치에 요약 데이터를 텍스트로 치환 삽입합니다.
    4. Gemini API 호출 시 `system_instruction`으로 주입하여 모델이 이 사실(Fact)을 기반으로 답변하도록 지시합니다.
  - **효과**:
    - **할루시네이션(환각) 원천 차단**: 모델이 모르는 정보를 지어내지 않고 주입된 시계열 수치에 근거해 답변합니다.
    - **5단계 표준 답변 아키텍처 강제**: 1. 연구현황, 2. 주요 연구주제, 3. 연구방법 변화, 4. 대표 논문 분석, 5. 최근 연구 방향으로 구조화된 전문가 리포트를 일관되게 생성합니다.

---

### Q6. 배포 환경에서의 CORS, 환경 변수, 보안 관리
**Q: 배포 환경에서 CORS, 환경 변수, API 키 관리가 왜 필요하며 어떻게 해결했나요?**
- **답변 핵심**:
  - **CORS(Cross-Origin Resource Sharing) 해결**: 프론트엔드는 Vercel(`https://*.vercel.app`), 백엔드는 Render(`https://*.onrender.com`)로 서로 도메인이 다르므로, 브라우저 보안 정책에 의해 API 요청이 차단됩니다. 이를 방지하기 위해 백엔드 `CORSMiddleware`에 `ALLOWED_ORIGINS="*"` 및 세부 헤더 허용 정책을 구성했습니다.
  - **API 키 및 인증 보안**: Gemini API 키(`GEMINI_API_KEY`)와 Firebase 서비스 계정 키를 코드베이스에 하드코딩하지 않고 `.env` 파일로 분리하고 `.gitignore`에 등록하여 GitHub 공개 노출을 차단했습니다. Render 및 Vercel 배포 시에는 플랫폼의 Environment Variables 메뉴에 안전하게 등록하여 운영합니다.
  - **유니코드 서러게이트 방어**: 대화 내역에 이모지나 특수문자가 포함될 때 JSON 직렬화가 깨지는 현상을 `_sanitize_obj` UTF-8 치환 로직으로 방어하여 서비스 무중단성을 확보했습니다.

---

## 3. 구현된 보너스 과제(가산점 항목) 및 기술적 차별점

본 프로젝트는 미션 기본 요건을 넘어선 **5가지 고도화 보너스 기능**을 완벽하게 구현했습니다:

| 구분 | 보너스 기능 명칭 | 세부 구현 내용 및 기술적 가치 |
| :--- | :--- | :--- |
| **보너스 1** | **시계열 인터랙티브 차트 시각화** | **Chart.js v4**를 연동하여 16개년 연구 데이터 지표 및 추세를 라인/바 혼합 차트로 시각화. 7대 주제 칩 클릭 시 차트와 통계가 실시간 리렌더링됨. |
| **보너스 2** | **데이터 내보내기 (Export 기능)** | 백엔드 `/api/data/export?format=csv` 및 `format=json` 엔드포인트를 구현하고, 프론트엔드 버튼 클릭 시 UTF-8 BOM 인코딩된 CSV 및 순수 JSON 파일을 즉시 브라우저에서 다운로드 지원. |
| **보너스 3** | **글래스모피즘 & 다크/라이트 모드** | 순수 CSS 커스텀 변수(`--bg-primary`, `--text-primary`)와 글래스모피즘(Glassmorphism, `backdrop-filter`)을 적용. 원클릭 토글 지원 및 `localStorage`를 통한 테마 상태 영구 보존. |
| **보너스 4** | **154편 구조화 논문 아카이브** | Tab 4에 154편의 수의학 연구논문 전용 검색 시스템 구축. 주제별, 연도별 필터링 및 키워드 실시간 검색, 상세 초록 및 표본수/방법론 모달 팝업 제공. |
| **보너스 5** | **Render 콜드스타트 완화 UX** | Render 무료 티어의 슬립 모드(첫 요청 시 30~50초 지연)를 고려하여, 상단 네비게이션 바에 **실시간 백엔드 연결 상태 감지 뱃지**와 헬스체크(`/api/health`)를 연동하여 사용자 이탈을 방지. |

---

## 4. 주요 엔드포인트 및 데이터 구조 요약

### 시계열 데이터 관리 (CRUD + 요약 + 내보내기)
- `GET /api/data`: 시계열 데이터 목록 조회 (주제별 필터링)
- `POST /api/data`: 새 데이터 항목 추가 `(date, value, memo, topic)`
- `PUT /api/data/{id}`: 기존 데이터 항목 수정
- `DELETE /api/data/{id}`: 데이터 삭제
- `GET /api/data/summary`: 시계열 통계 요약 (기간, 개수, 평균/최대/최소, 트렌드 증감률, 연구기법)
- `GET /api/data/export`: CSV 및 JSON 다운로드

### AI 대화 및 대화 기록
- `POST /api/chat`: 데이터 요약 컨텍스트 주입 질의응답 및 대화 자동 저장
- `GET /api/conversations`: 저장된 대화 세션 목록 조회
- `GET /api/conversations/{id}`: 특정 대화 전체 메시지 불러오기
- `DELETE /api/conversations/{id}`: 대화 세션 삭제

### 연구논문 아카이브
- `GET /api/papers`: 154편 논문 검색 및 목록 필터링
- `GET /api/papers/{id}`: 특정 논문 상세 메타데이터 조회

---

## 5. 면접/평가 예상 꼬리질문 대비 FAQ

### Q. 프론트엔드에 React나 Vue 같은 프레임워크를 쓰지 않고 Vanilla JS를 쓴 이유는?
> **답변**: 과제 요구사항인 '순수 웹 표준(Vanilla HTML/CSS/JS)' 제약조건을 철저히 준수한 것입니다. 불필요한 번들러(Webpack/Vite) 오버헤드 없이 브라우저 네이티브 DOM API와 이벤트 델리게이션, Fetch API만으로 SPA(Single Page Application) 급의 탭 전환과 반응형 상태 관리를 직접 구현할 수 있는 기초 역량을 증명했습니다.

### Q. Gemini 3.5 Flash 모델을 선택한 이유는?
> **답변**: 현재 구글 생성형 AI 환경에서 구버전인 `gemini-1.5-flash`와 `gemini-2.5-flash`는 지원 종료 또는 404 NOT_FOUND를 반환합니다. 이에 따라 최신 활성 모델인 `gemini-3.5-flash`를 기본 모델로 채택하고, 순간적인 고부하(503 High demand) 발생 시 자동으로 `gemini-3.6-flash`로 우회하는 모델 페일오버(Fallback) 로직을 구축하여 가용성을 극대화했습니다.

### Q. 대화 불러오기(Restore)는 어떻게 동작하나요?
> **답변**: `Tab 3 (대화 기록)`에서 '대화 불러오기' 버튼을 클릭하면, `/api/conversations/{id}`를 호출하여 해당 세션의 전체 메시지 배열을 수신합니다. 그 후 현재 세션 ID(`state.currentConvId`)를 해당 대화 ID로 갱신하고, 메인 채팅 화면의 메시지 스트림을 비운 뒤 저장되어 있던 유저 및 AI 메시지를 순서대로 재렌더링하여 이전 대화를 끊김 없이 이어나갈 수 있도록 구현했습니다.
